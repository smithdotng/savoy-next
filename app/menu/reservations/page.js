import ReservationsList from '@/components/admin/ReservationsList';

export const metadata = {
  title: 'Reservations - Savoy Summerset'
};

export default function ReservationsPage() {
  return <ReservationsList />;
}
